# STATE.md — three-dbox

## Current phase

**Phase 2: Character & Input Polish** — IN PROGRESS (2026-04-13), **DEFERRED** pending Phase 3 physics foundation.
**Phase 3: OW Map + Physics** — IN PROGRESS (2026-06-07). T-B13 ✅ T-B14 ✅ T-B15 ✅ T-B16 ✅. OW map POC COMPLETE (2026-06-08). **Tier 1 fixes SHIPPED (2026-06-08)** — Fix 1 dual-sphere wall penetration ✅ Fix 2 normalY step-climb ✅ Fix 3 MapDescriptor + spawn ✅ IAbilityLab/DoomfistLab ✅. **Health pack substructure SHIPPED (2026-06-08)**. **Spawn calibration DONE (2026-06-09)** — 4 ground-level points, elevated point removed. **T-B17 round structure SHIPPED (2026-06-09)** — `RoundManager` countdown→playing→ended + timer overlay + end screen + pointer-lock release. **Systems assessment 2026-06-10** (`docs/ASSESSMENT-2026-06-10.md`) — architecture sound; 3 defects + fix plan locked. **Spawn system V1 planned (2026-06-10, `docs/PLAN-SPAWN-2026-06-10.md`)** — SP-1 decouple spawn data from health-pack entities (rides Tier A), SP-2 instant respawn with rotation at HP 0 (rides B1); death cam = Phase 5. **Tier A (A1–A4) + SP-1 SHIPPED (2026-06-12)** — sim-time cooldowns ✅ pack rotation cycles ✅ truthful sandbox fallback ✅ `entityScan.ts` + spawn decoupling ✅ (0345 survey: LOCATED via second naming family `Entity_<12hex>`, both at one spot → gate fallback branch, colocated points nudged 3 m). All verified live. **Planning session 2026-06-12** — multi-champion confirmed near-term (C2-first); owner playtest verdict (lag / fall-through / stairs / no audio / over-bright lighting) folded into a new **EX experience-floor track sequenced AHEAD of all mechanics**. Sprint primed: `docs/SPRINT-EX-V1.md`. **EX-1 SHIPPED (2026-06-13, branch `feat/ex1-perf-diagnosis`, commit 89577e7, local)** — flag-gated `?perf`/`?navdebug` diagnostics + pixel-ratio cap `min(dpr,2)`; findings `docs/EX-1-FINDINGS.md`. Measurement **revised the hypothesis**: the lag driver is the **960K-tri player mesh `dfist_base.glb` (73% of scene geometry, un-decimated Tripo export)**, NOT draw calls (120–180 typ / 463 worst); render is vertex-bound on the dev GPU (flat across pixelRatio → cap is hi-DPI insurance). **BatchedMesh deferred.** **EX-1.5 SHIPPED (2026-06-13)** — player mesh `dfist_base.glb` decimated **960,108 → 38,404 tris** (gltf-transform `weld` + `simplify --ratio 0.04 --error 0.01` + `draco`; 3.05 MB → 639 KB); skin weights + `JOINTS_0`/`WEIGHTS_0` preserved, 40-bone skeleton **mixer-verified live** (clean idle deformation, sane bbox, no console errors). The EX-1 #1 perf lever is resolved; the character is now ~normal game budget, so EX-2's frame numbers will be honest. Kept Draco (meshopt breaks GPU skinning); no normals added (EX-3 owns shading). Pre-sprint git gate resolved (PR #5/#6 already merged; #7 docs open). **EX-2.1 SHIPPED (2026-06-14)** — anti-tunnelling: additive `PhysicsWorld.shapeCastSphere(from,to,radius)` in `@base/physics` (Rapier `castShape`; needs `updateSceneQueries()` in this never-stepped query world or it returns null; normal flipped to face the caster; **unit-proven 8/8** via a thin-wall esbuild harness) + swept pre-pass in `DboxCharacterEntity.resolveCollision` gated on per-tick move > 0.5×radius (walking never pays it). physics rebuilt → dbox typecheck clean → Château map-mode smoke clean (spawn correct, `posFinite`, no errors, swept ticks without NaN). Fall-through confirmation = **owner playtest** (152 m/s punch can't be driven headless). **EX-2.2 probe-Y ATTEMPTED → REVERTED (2026-06-14).** Lowering the steady terrain-probe origin (`+1.0` above the head → `−0.30` below the capsule centre) to kill the stair/overhang pop **regressed grounding** and was reverted to the known-good `+1.0`. Root cause (confirmed live by A/B): a probe origin that tracks `position.y` from below chases the character DOWN on any dip and can't re-acquire the floor → grounding **diverges**, the avatar sinks/oscillates, the camera lurches — surfaced as a **load "flicker"** (owner-reported; reproduced headless as a monotonic spawn fall, then `charY`-span 0 once `+1.0` restored). The high origin is self-correcting (~1.85 m recovery zone above the floor). Crouch also breaks a low probe independently (`DEFAULT_SKINNED_CROUCH_TERRAIN_Y_DELTA = −0.45` drops the crouched centre to floorY+0.40). **Lesson:** the real fix must be **anti-divergent** (clamp the origin to the last known floor — don't chase `position.y`) AND **owner-verified on stairs** — exactly the interactive bug-hunt the plan flagged; do NOT change the probe blind. EX-2.1 anti-tunnelling is unaffected and stands. **Then two more owner-reported issues addressed (2026-06-15) — both IMPLEMENTED + OWNER-VERIFY PENDING, uncommitted WIP:** (1) **floor-tile stumble/stuck** — the Rapier feet collision sphere sat at floor level and caught every OW-floor tile seam (micro step-lifts + XZ pushes); raised it `FOOT_PROBE_CLEARANCE = 0.12` m off the feet (the top-down `MeshTerrainSampler` owns floor height, so the feet sphere should only catch walls/steps). Safe — only reduces near-floor collision sensitivity; static grounding unchanged. Needs owner walk-test. (2) **spawn "under textures" on remount** — *structural* fix (plan `~/.claude/plans/typed-crafting-wilkinson.md`): an explicit `mapRoot.updateMatrixWorld(true)` barrier before sampler/spawn (correctness was an implicit side-effect of `addStaticMesh` + render timing → could differ first-mount vs remount); **single grounding offset** — spawn now uses `characterTerrainYOffset` (feet-aligned GLB = 0), not a hardcoded `+0.85`, so spawn == steady (kills a float-then-snap); a dev guard on non-finite floorY; a `?navdebug` SPAWN trace. Build clean; **headless verify with RAF actually running: sampler `gap=0` (feet on floor, stable).** Remount visual = **owner Play-Again ×N** (Play Again preserves `?navdebug`; the menu cards drop it). Caveat: headless raycast reconstructions are contaminated by an invisible SceneBuilder Y=0 ground-plane the sampler correctly ignores → trust the nav-log `gap`, not pixel reads. **Next: owner verify remount + floor-stumble in real browser → EX-2.2 anti-divergence probe-Y redesign WITH owner stair-walking → step-lift smoothing if needed → EX-3 lighting → EX-4 audio → C2 → B1+B2+SP-2 → B3+B4 → T-B18.**
Phase 1: Wall Collision & Slide — COMPLETE (2026-04-12).

### Phase 2 progress (2026-04-13)

- [x] **HUD overlay** — OW1-style bottom-center bar: character portrait ("D" placeholder) + health bar (left), 4 ability slots with cooldown sweep/numbers (right)
- [x] **Dynamic key labels** — HUD reads active bindings from settings, not hardcoded keys
- [x] HUD architecture: DboxLab.getHudAbilities() → DboxSceneModule.getHudSnapshot() → DboxView rAF poll → DboxHud.vue (pure display, no game logic)
- [x] Dummy health (250/250) + shields (0/150) ready for real health system
- [ ] Health/damage system
- [ ] Visual effects for abilities (punch trail, slam impact, uppercut arc)
- [ ] Crosshair

Forked from `threejs-engine-dev` dbox locomotion lab into standalone project.
- 26 source files, 2 GLB assets (dfist_base.glb 1.5MB, animations_base.glb 728KB)
- `@base/*` packages linked via `link:../SHARED/packages/...`
- TypeScript check + Vite build both pass clean
- Routes: `/` (menu), `/dbox` (gameplay), `/settings`

## What works

- [x] Doomfist character loads and animates (dfist_base.glb + animations_base.glb)
- [x] WASD movement at 5.5 m/s + Shift sprint + Space jump + C crouch
- [x] Rocket Punch: RMB hold 0–1.4s → release → launch at 78–152 m/s + small vertical lift
- [x] Rising Uppercut: Q → forward + upward impulse, cone-hits nearby blobs (0.6s lock)
- [x] Seismic Slam: E hold → mouse-aim cone preview → release → dash to apex + slam down AoE
- [x] Skim-jump: Space during punch carry → extends travel into arc
- [x] 5 NPC blobs react to uppercut (lift + lock) and slam (knockback)
- [x] Camera FPV/TPV toggle via Tab
- [x] Time control: P pause, F step-frame, R resume, [ ] slow/fast
- [x] Terrain: 100x100m with pool, 5 landing ramps (2–22m), obstacles (knee/body height)
- [x] Input settings: configurable bindings, pointer lock
- [x] HUD overlay: health bar + ability cooldown display (bottom-center)
- [x] OW map GLB pipeline: Château Guillard (Open3DLab → gltf-transform draco+webp → 8.8MB, `public/maps/chateau-guillard.glb`)
- [x] Rapier trimesh collision: `DboxSceneModule` loads map → `PhysicsWorld.addStaticMesh()` with `smd_bone_vis` filter → `spherePenetration` in `DboxCharacterEntity` (walls only, `|normalY| < 0.7`)
- [x] Scene cleanup in OW map mode: sandbox geometry (grid/platforms/pool), NPC blobs, analytic arena walls, and fixture legend panel all suppressed; `useSandboxScene()` hook + `spawnBlobs` opt
- [x] Terrain sampler: `MeshTerrainSampler` with `() => Math.max(character.position.y + 1.0, 1.0)` — probe from 1 m above character (tracks player elevation for multi-level geometry) with a floor clamp at Y=1.0 (self-corrects below-floor states; at spawn character.y≈0 → probe=1.0 = proven baseline). Rooftop (Y≈81.5) unreachable until character.y > 80.5. Ceilings auto-skipped (Three.js FrontSide). Spawn at `(30, −15)` east-wing interior, `floorY=−0.969`, `spawnY=−0.119`
- [x] OWLib technical mesh filter: `smd_bone_vis` excluded from Rapier + sampler; ≤8-tri spawn/trigger quads hidden (kept in terrain); material-type filter `/\b(13A|9F|9C|9A|B2|10F|F0)_/` hides effect volumes; count logged via `console.debug` at load
- [x] **Round structure (T-B17, 2026-06-09)**: `RoundManager` countdown (3s) → playing (60s) → ended state machine; top-center timer; countdown overlay (3/2/1 + GO! flash); end-screen with Play Again / Menu; pointer lock released on round end; `restart()` via menu detour with arena query captured pre-navigation
- [x] Arena select menu: `MenuView` with Château Guillard (violet) + Sandbox (cyan) buttons; route via `?arena=chateau-guillard` query param
- [x] Central arena registry (`src/arenas/registry.ts`): adding a new map is 2-file work — create `src/maps/<name>.ts` + one registry entry; `MenuView` + `DboxView` both derive from registry; route uses `RouteLocationRaw` named routes (base-path safe)
- [x] Data-first `MapDescriptorData` schema: OWLib vocabulary (spawn points with zone labels, physics exclude patterns, owlib hidden type codes + entity type annotations, display overrides); `compileMapDescriptor()` bridges to runtime `MapDescriptor` with pre-compiled RegExp
- [x] Blender collection hierarchy baked into descriptor: headless Blender 5.0 extraction (`extract_collections.py`), `BlenderCollection` type with role/exclusiveMeshHashes/notes; documents 201 mountain meshes, 6 fog cubes, 5649-node entity collection
- [x] Zone display overrides: second traversal pass in `DboxSceneModule.onMount()` runs on ALL meshes (including Rapier-excluded background geometry); `meshDisplayOverrides` hides mountain hashes + fog cubes (`mesh.visible = false`); 13 meshes hidden in Château Guillard
- [x] **Three.js node-name sanitization fix**: `PropertyBinding.sanitizeNodeName()` strips dots from GLTF node names — Blender `Submesh_0.HASH.443` → `Submesh_0HASH443`. Physics `excludeNamePatterns` and `display.namePatterns` updated to bare hash substrings; `^Cube\d*$` replaces `^Cube(?:\.\d+)?$`
- [x] **Tier A correctness fixes (2026-06-12)**: A1 ability cooldowns on sim time (`DoomfistLab.simTimeS` accumulated in `afterGameplayTick`; `lastPunchSimS`/`lastUppercutSimS`/`lastSlamSimS`; pause freezes CDs — verified live with PAUSED pill + frozen HUD number). A2 health-pack rotation cycles via module-scope `packRotationCounter` (survives fresh-module-per-view remounts; observed rotations 0→14 across SPA detours, all 3 layouts cycling). A3 map GLB loads BEFORE `super.onMount()` — load failure clears `this.map` so `useSandboxScene()` flips true and the fallback is the real calibration arena (verified live with broken URL → playable sandbox + `console.error`). A4 keymap panel CD numbers sourced from `DOOMFIST_CONFIG`
- [x] **Generic entity node scan (SP-1, 2026-06-12)**: `src/maps/entityScan.ts` — `extractEntityNodes(root, types?)` matches BOTH OWLib naming families (`0000000037C2.001` item markers AND `Entity 000000000345.001` pure-EMPTY game entities; `_WRAP` parents carry translation, inner nodes matched, `ENTITY_NODE_RE = /^(?:Entity_)?00000000([0-9A-Fa-f]{4})(\d{3})$/`). `HealthPackExtractor` refactored onto it (public API unchanged). `debugEntityTypes?: string[]` descriptor flag renders magenta spheres + `[entity]` console lines for EMPTY entities invisible to the ≤8-tri mesh survey
- [x] **Spawn data decoupled from pack entities (SP-1, 2026-06-12)**: invariant — every spawn point >1.5 m XZ from every configured pack slot (worst case now 3.0 m). `mainhall-corridor-west (9.8,-12.3)` / `mainhall-corridor-east (20.6,-12.1)` replace the 37C2-colocated points (nudged 3 m along corridor axis, both verified standing live). `SpawnPoint.yaw?` added (applied by SP-2 `placeAtSpawn`); runtime `spawnPoints` is now the authored struct (labels kept); spawn pick logged as `[DboxSceneModule] spawn at '<label>'`

## Known issues

- Character scale fixed: `modelFitHeight` set to 2.1 m (OW1 Doomfist ~7 ft), was inheriting sandbox Remy 1.78 m
- Spawn set **DECOUPLED 2026-06-12 (SP-1)** — 4 ground-level points: `yard-center (9.4,1)`, `mainhall-corridor-west (9.8,-12.3)`, `mainhall-corridor-east (20.6,-12.1)`, `otherparts-ground-east (19.6,-12.6)`; all >1.5 m XZ from every configured pack slot. 0345 spawn-volumes located via `entityScan` — both at `(-19.5, 2.0, -1.7)±0.3` (OW spawn-room anchor pair, too clustered for deathmatch spawns → plan gate fallback branch; recorded in descriptor `entityTypes`). 37C2 packs confirmed on the UPPER corridor (Y≈6.9), not ground floor. `mainhall-corridor-east` sits ~1.1 m from `otherparts-ground-east` (spawn-to-spawn, allowed; revisit if spawn variety feels low). Elevated spawns still deferred (terrain probe starts at Y=1).
- NPC blobs collide with walls (bounce) but have no health/damage system
- Meteor Strike not implemented
- **Perf: player mesh `public/models/dfist_base.glb` is 960K triangles** (Tripo export, `tripo_node_*`, 73% of scene geometry) — EX-1.5 decimation target (~30–50K). Alt-skins `dfist.glb`/`dfist_armored.glb` (33 MB each, untracked) even heavier. *(EX-1, 2026-06-13)*
- **Latent: `@base/input` TouchProvider stamps inline `position:relative` on the engine container at mount**, defeating Tailwind `absolute inset-0` → container `h:0` → 0×0 canvas → black screen if `ThreeModule` reads `clientHeight` before the canvas sizes (a race; reproduces reliably headless, owner's browser wins it). Mount-side sibling of the 2026-06-02 unmount-restore fix. **RESOLVED 2026-06-13** — `@base/input` `TouchProvider.mount` now guards on `getComputedStyle(container).position === 'static'` before stamping `relative`, so a class-positioned (`absolute`/`relative`/`fixed`/`sticky`) container is never overridden. Verified live (`/dbox?arena=chateau-guillard`): container stays computed `absolute`, `clientHeight` 1295 px, canvas non-zero (1111×1295), touch overlay fills play area; `@base/input` rebuilt + dbox typecheck clean. *(EX-1, 2026-06-13)*
- **Assessment findings 2026-06-10** (full register + fix plan: `docs/ASSESSMENT-2026-06-10.md`):
  - ~~F1~~ **FIXED (A2, 2026-06-12)**: health-pack rotation cycles via module-scope `packRotationCounter`
  - ~~F2~~ **FIXED (A1, 2026-06-12)**: ability cooldowns on sim time — pause freezes, slow-mo scales with round timer
  - ~~F3~~ **FIXED (A3, 2026-06-12)**: GLB load moved before `super.onMount()`; failure → genuine sandbox fallback
  - F4: round state is presentational only — no input gating during countdown/ended (fix B4)
  - F5: dual-sphere probe waist window (~±5 cm at capsule centre) — thin horizontal edges can slip; capsule query is the upgrade (C1, triggered)
  - F8: forked `GameplaySceneModule` predates engine-dev's 2026-06-02 remount/disposal fixes — GPU disposal gaps on unmount (C4, triggered)
  - Process: `main` frozen at initial commit (all work on `docs/phase2-sync-2026-04-13`, pushed); `public/maps/chateau-guillard.glb` untracked — exists only on this machine (P1/P2)
- Arena has 80×80m boundary walls, 3 angled interior walls, 2 pillars
- Input remapping for abilities not exposed in settings UI
- `PlayerController.isLandingTooHigh()` fires for any landing surface > 0.85 m above takeoff ground — prevents clean Uppercut landing on upper floors (secondary, ground-floor gameplay unaffected)

## Blockers

No current blockers. Tier A (A1–A4) + SP-1 ✅ shipped 2026-06-12. **Plan of record re-sequenced 2026-06-12 (planning session):** owner playtest verdict inserts the **EX experience-floor track ahead of mechanics** → `docs/SPRINT-EX-V1.md` is the session-series tracker. Order: pre-sprint git gate → EX-1 (perf/glitch diagnosis + renderer quick wins) → EX-2 (anti-tunneling swept cast in `@base/physics` + probe-Y/stairs fix) → EX-3 (per-map lighting schema + Château dusk) → EX-4 (audio) → C2 (target-system extraction + champion registry) → B1+B2+SP-2 → B3+B4 → T-B18. **OD-1..OD-3 still resolve at the top of B1** (fall-damage wiring, blob KO, ability damage numbers). Mechanics plans unchanged: `docs/ASSESSMENT-2026-06-10.md` + `docs/PLAN-SPAWN-2026-06-10.md` + `docs/PLAN-C2-CHAMPIONS-2026-06-12.md`; gap analysis: `docs/GAP-V1-PUBLISH-2026-06-12.md`.

## Roadmap restructure (2026-06-06)

Original roadmap (Phases 0–5) updated — two new phases inserted after assessment:

- **Phase 2 close** — 5 remaining items: health/damage system, crosshair, screen shake/hit-stop (T-B10), ability remapping in settings, uppercut ceiling clamp (Phase 1 deferred)
- **Phase 2B (new)** — Ability completeness: Hand Cannon (T-B8), Passive: The Best Defense (T-B9), ability combos (T-B11), Meteor Strike (optional)
- **Phase 3 reframed** — OW map GLB pipeline (Château Guillard / Ecopoint: Antarctica) + `@base/physics` Rapier package; replaces hand-authored arena geometry
- **Phase 4** — Round flow (unchanged)
- **Phase 5** — Polish + sound (T-B12) + PWA ship (unchanged)

New targets: T-B13 `@base/physics` package · T-B14 OW map GLB pipeline · T-B15 Rapier trimesh collision in dbox · T-B16 Arena select menu · T-B17 Round structure · T-B18 Results screen

## Assets inventory

| Asset | Path | Notes |
|-------|------|-------|
| Doomfist mesh | `public/models/dfist_base.glb` | Trinity rig, meshopt + WebP |
| Doomfist alt | `public/models/dfist.glb` | Earlier export, untracked |
| Doomfist armored | `public/models/dfist_armored.glb` | Armored skin, untracked |
| Locomotion anims | `public/characters/npc/animations_base.glb` | Shared pack, indices: idleStand=4, walkFwdStand=6, runFwdStand=3 |

## Decision log

| Date | Decision |
|------|----------|
| 2026-07-05 | **CI review agent LIVE on main + typecheck de-vacuumed (PRs #10, #12 merged; harness session, no gameplay code).** `claude-code-review.yml` (auto PR review, dbox-tailored: tick-order / render-loop allocs / disposal / package boundary) + `claude.yml` (@claude mentions) on main with `CLAUDE_CODE_OAUTH_TOKEN` set; posting proven end-to-end on throwaway PR #13 (closed). `typecheck`/`build` scripts fixed `vue-tsc --noEmit`→`-b` — the old form checked NOTHING against the solution tsconfig, so **no dbox production build was ever typechecked** (memory `reference_vue_tsc_solution_tsconfig`). Consequence now visible: **main's Deploy Pages fails at typecheck** on a real pre-existing skew — main's `DboxSceneModule.ts:128` calls `addStaticMesh(root, physicsFilter)` (2-arg) but builds against 1-arg `@base/physics` (2-arg lives on unmerged SHARED `feat/base-physics`). Not a regression (deploys already red since 2026-06-12 via a vite-plugin-pwa error); resolves when the physics + EX branches merge. Open PRs #7/#9 get auto-review on next push. |
| 2026-06-15 | **EX-2.2 low-probe REVERTED + floor-stumble & remount-grounding fixes IMPLEMENTED (owner-verify pending, all uncommitted WIP).** (1) **EX-2.2**: lowering the steady terrain probe `+1.0`→`−0.30`/`−0.5` to kill the stair pop **regresses grounding** — a `position.y`-relative origin chases the avatar DOWN on a dip and can't re-acquire the floor (divergence → sink/oscillate → owner-reported "flicker"); A/B-proven (revert to `+1.0` = `charY`-span 0). Real fix must be **anti-divergent** (clamp to last-known floor) + owner-verified on stairs; memory `reference_terrain_probe_crouch_offset`. (2) **Floor-stumble/stuck**: the Rapier feet sphere sat at floor level → caught every OW tile seam (micro step-lifts + XZ pushes). Decision: the top-down `MeshTerrainSampler` owns floor height, so the feet sphere should only catch walls/steps → raised it `FOOT_PROBE_CLEARANCE = 0.12` m off the feet (`DboxCharacterEntity`). (3) **Remount "under textures"** (plan `typed-crafting-wilkinson.md`): root cause = the map-mode build sequence is implicit/timing-coupled — grounding correctness rode on `addStaticMesh`→`extractTrimesh` updating `matrixWorld` as a side effect + render-frame timing (→ differed first-mount vs remount), and spawn used a hardcoded `+0.85` while steady grounding uses `characterTerrainYOffset` (=0 for the feet-aligned GLB) → inconsistent. Structural fix: explicit `mapRoot.updateMatrixWorld(true)` barrier before sampler/spawn; spawn uses `characterTerrainYOffset` (spawn==steady, no float-then-snap); non-finite-floorY guard; `?navdebug` SPAWN trace. RAF-running headless verify = sampler `gap=0` (grounded, stable); remount visual = owner Play-Again ×N. Caveat: headless raycasts are contaminated by an invisible SceneBuilder Y=0 ground-plane the sampler ignores → trust the nav-log `gap`, not pixel reads. |
| 2026-06-14 | **EX-2.1 anti-tunnelling SHIPPED** (SHARED `feat/base-physics` 8372cf2 + dbox `feat/ex1-perf-diagnosis` 6586928, local). New additive `PhysicsWorld.shapeCastSphere(from,to,radius): {toi,normal}\|null` (Rapier `castShape`, Ball, maxToi=1 → toi∈[0,1], contact = `from.lerp(to,toi)`). **Two non-obvious fixes a thin-wall harness caught:** (1) the never-stepped query world doesn't populate the query pipeline for `castShape` → returns null until `addStaticMesh` calls `world.updateSceneQueries()` (the existing `spherePenetration`/`castRayDown` work pre-update — different internal path); (2) Rapier's cast `normal2` isn't caller-facing → flip it against the sweep direction so it's always a valid push-out/slide normal. **Lesson: null-asserting tests give false confidence** (can't distinguish working-clear from always-null) — added a positive overlap control → 8/8 pass. Consumer: `DboxCharacterEntity.resolveCollision` swept pre-pass gated on per-tick move > 0.5×radius (walking ≤0.09 m/tick never pays it) → wall hit (\|n.y\|<0.7) → clamp to contact + 0.02 m skin + existing slide; `lastPosition` tracked at end of `resolveWalkingCollision` (the tick's last resolve). dbox typecheck + Château map-mode smoke clean (spawn ok, `posFinite`, no errors, swept ticks without NaN). **Fall-through confirmation = owner playtest** (152 m/s punch not drivable headless). **EX-2.2 deferred deliberately:** probe-Y (`+1.0`→`−0.5`, `DboxSceneModule.ts:196`) is the flagged bug-hunt with spawn/grounding-regression risk — needs interactive stair verification + the `?navdebug` trace; changing it blind risks a regression I can't catch. Memory: `reference_rapier_query_world_gotchas`. SHARED branch also carries pre-existing unrelated `extractTrimesh.ts` WIP — left untouched. |
| 2026-06-13 | **EX-1.5 char-mesh decimation SHIPPED** (`feat/ex1-perf-diagnosis` 86db4d3, local). `dfist_base.glb` 960,108 → 38,404 tris (gltf-transform `weld` → `simplify --ratio 0.04 --error 0.01` → `draco`; 3.05 MB → 639 KB), resolving EX-1's #1 perf lever (the un-decimated Tripo mesh was 73% of scene geometry). **Kept Draco, NOT meshopt** — meshopt `KHR_mesh_quantization` renormalizes POSITION to [-1,1] → breaks GPU skinning vs the inverse bind matrices (constraint already documented in `src/scenes/dbox.ts`). **No normals added** (faithful to the flat-shaded source; EX-3 owns shading). Skin weights + `JOINTS_0`/`WEIGHTS_0` preserved; 40-bone skeleton mixer-verified live via `?perf`/`window.__dboxPerf` (38,404-tri SkinnedMesh, non-identity bone quaternions = idle clip driving the rig, clean deformation, sane local bbox 0.46×1.0×1.0 = no broken-weight explosion). `prune` was a no-op (runtime char subtree = 44 clean nodes — the raw file's many unused *scenes* never load; the "15,400 nodes" was an inspect-table misread). Alt-skins `dfist.glb`/`dfist_armored.glb` (33 MB each, untracked) need the same pass before activation. Memory: `reference_skinned_glb_decimation`. |
| 2026-06-13 | **EX-1 perf+glitch diagnosis SHIPPED** (`feat/ex1-perf-diagnosis`, commits 89577e7 + 42612c8; CodeReview clean). `src/debug/diagnostics.ts` (flag helpers + `CountingSampler` + `PerfOverlay`), `?perf` 1 Hz `renderer.info` overlay + dev-only `window.__dboxPerf` handle, `?navdebug` per-tick nav logger, `DboxCharacterEntity.lastStepLiftY/lastWallPushDepth`. **Renderer quick win shipped: pixel-ratio cap `min(dpr,2)`** (`?pixelratio=N` override) — unconditional, no visual regression (`dpr=1` dev display → no-op); tone/shadow left at defaults + logged for EX-3. **Measurement revised the 2026-06-12 hypothesis** (`docs/EX-1-FINDINGS.md`, numbers read straight from `renderer.info` via the handle → build-independent): the lag driver is **NOT unbatched draw calls** (120–180 typical / 463 worst-case courtyard view) — it is the **player mesh `dfist_base.glb` at 960K triangles (73% of scene geometry, un-decimated Tripo export `tripo_node_*`)**, plus fragment cost on hi-DPI displays. Render time is **flat across pixelRatio 1/2/3 (~10.3 ms) on the discrete dev GPU → vertex/submit-bound, not fill-bound**, so the cap is insurance for fill-bound integrated/hi-DPI hardware (the owner's likely lag), not the dev-machine lever. Stair pop (probe-Y `+1.0` vs sampler contract `−0.5`) + wall fall-through (`spherePenetration` overlap-only vs 152 m/s punch) both root-caused by code for EX-2. Lighting baseline captured (Château inherits sandbox **noon** atmosphere, `NoToneMapping`). **Decisions: (1) defer `BatchedMesh`** — draw calls aren't the bottleneck; revisit only if the 463-draw view still costs frames after the mesh fix. **(2) NEW EX-1.5 task inserted before EX-2: decimate `dfist_base.glb` 960K → ~30–50K** (gltf-transform weld+simplify, preserve skin) — highest-ROI perf win, unblocks honest frame-time. **Bonus bug found:** `@base/input` TouchProvider stamps inline `position:relative` on the engine container at mount → defeats Tailwind `absolute inset-0` → `h:0` → 0×0 canvas (race; reproduces headless, owner's browser wins it). Mount-side sibling of the 2026-06-02 unmount fix; flagged as a follow-up task. |
| 2026-06-12 | **Planning session — V1 direction + EX experience-floor track (no code).** Three decisions. (1) **Multi-champion confirmed near-term intent** → C2 target-system extraction pulled forward ahead of B1 (B1 would write blob HP into the exact `DoomfistLab` code C2 extracts); plan `docs/PLAN-C2-CHAMPIONS-2026-06-12.md` (new `src/targets/TargetSystem.ts` owned by orchestrator + `CHAMPION_REGISTRY` mirroring `ArenaEntry` + `setWallGeometry` off `IAbilityLab`). Maps axis needs **no** system work — registry + `MapDescriptorData`/`compileMapDescriptor` + `entityScan` already sufficient; only per-arena counter keying remains (rides C2). (2) **V1 goal scoped** = OW1 experience in limited form: Score Attack (Château 60 s + stats) + Practice Range (sandbox), single champion; deploy + PWA found **already wired** (`deploy-github-pages.yml` + vite-plugin-pwa) — kills 2 Phase-5 roadmap items. Gap + leverage analysis `docs/GAP-V1-PUBLISH-2026-06-12.md`. (3) **Owner playtest verdict reprioritizes the floor** (lag / fall-through-walls / stair+transition pops / no audio / over-bright lighting). Root causes code-verified: lag = uncapped `setPixelRatio(devicePixelRatio)` (`threejs-engine ThreeModule.ts:42`) + ~374 unbatched map draw calls + no-BVH linear `MeshTerrainSampler`; fall-through = `spherePenetration` overlap-only vs 152 m/s punch (tunneling, self-documented limitation); stairs = probe-Y `+1.0` (`DboxSceneModule.ts:168`) contradicts sampler contract "below ceiling, ~`y−0.5`" (`MeshTerrainSampler.ts:38`); lighting = `MapDescriptorData` has no lighting field, Château inherits sandbox atmosphere. New **EX track sequenced ahead of all mechanics** (EX-1 diagnosis+quick-wins → EX-2 navigation reliability → EX-3 lighting → EX-4 audio); plan `docs/PLAN-EX-EXPERIENCE-FLOOR-2026-06-12.md`, sprint tracker `docs/SPRINT-EX-V1.md`. EX-1/EX-3 dbox-local (`ctx.renderer`/`ctx.scene` exposed); EX-2 adds `shapeCastSphere` to `@base/physics` (SHARED, additive); EX-4 consumes `@base/audio` unchanged. T-B18 slips ~4 sessions — accepted: tuning on a janky base produces non-transferable numbers. |
| 2026-06-12 | **Tier A (A1–A4) + SP-1 SHIPPED** (branch `feat/tier-a-spawn-decouple`, PR #6). A1: `DoomfistLab` accumulates `simTimeS` in `afterGameplayTick(simDelta)`; all CD bookkeeping (`lastPunchSimS`/`lastUppercutSimS`/`lastSlamSimS`, `getHudAbilities`) reads it — verified live (PAUSED pill + CD frozen 1.5 s, resumes 1:1; punch *charge* duration stays wall-clock by design, comment in code). A2: module-scope `packRotationCounter` read-then-incremented where packs mount — verified rotations 0→14 across SPA detours, all 3 layouts observed. A3: GLB load + `PhysicsWorld.create()` moved BEFORE `super.onMount()`; failure clears `this.map` (now non-readonly) → `useSandboxScene()` true → real calibration arena — verified live with broken URL (console.error + playable sandbox screenshot). A4: keymap CD/charge/lock numbers from `DOOMFIST_CONFIG`; stale `\.HASH\.` + entity-name comments fixed. **SP-1 survey reversal #2**: 0345/0ED2 ARE in the GLB but under a SECOND naming family — raw `"Entity 000000000345.001"` + `"..._WRAP.001"` (WRAP carries translation, inner node carries Z-up→Y-up quaternion; sanitizes to `Entity_000000000345001`) vs item-marker family `"0000000037C2.001"` → `0000000037C2001`. `ENTITY_NODE_RE` extended with optional `Entity_` prefix; `_WRAP`/`_COLLECTION` excluded by the trailing-digits anchor (no pack-slot duplicates — still 6/6). **Gate → fallback branch**: both 0345 volumes at `(-19.5, 2.0, -1.7)±0.3` — the OW spawn-room anchor pair, too clustered for FFA spawns. 37C2-colocated points nudged 3 m W along corridor → `mainhall-corridor-west (9.8,-12.3)` / `-east (20.6,-12.1)`, both verified standing live; invariant >1.5 m XZ from all 6 pack slots holds (worst 3.0 m). 37C2 Y ambiguity resolved: packs on UPPER corridor Y≈6.9 ("ground floor" comments were wrong). `SpawnPoint.yaw?` + runtime struct landed for SP-2; spawn picks now logged. New tool: `scripts/dump-glb-node-names.cjs` (GLB JSON-chunk node-name census). |
| 2026-06-10 | **Spawn system V1 plan locked** (`docs/PLAN-SPAWN-2026-06-10.md`). Scoping found the 2026-06-09 calibration coupled player spawns to health-pack data: `mainhall-interior-west/east` are XZ-identical to the 37C2 pack entity nodes (calibration reused item-marker positions), spawn selection is random-per-mount with no respawn path, and player HP-0 behaviour was unspecified in the assessment plan. Key reversal: entity `0345` OW spawn-volumes ARE locatable — `debugMarkers` only surveys ≤8-tri meshes, but the HealthPackExtractor name-scan works on EMPTY nodes; SP-1 generalizes it into `src/maps/entityScan.ts` + `debugEntityTypes` descriptor flag + live survey gate (adopt 0345 positions, else nudge colocated points). Decoupling invariant: every spawn point >1.5 m XZ from every configured pack slot. `spawnCount` → module-scope `packRotationCounter` (= assessment fix A2). SP-2 (rides B1): module-scope `playerSpawnCursor` round-robin from random start shared by mount + death; `placeAtSpawn()` extracted from onMount; `respawnPlayer()` at HP≤0 = reposition + full heal + `RoundStats.deaths++` + zero planar carry (`setPlanarCarryVelocity(0,0)` — punch carry must not survive the teleport); round timer untouched; no spawn protection. **Locked: V1 = instant respawn with rotation; death cam = Phase 5 polish.** Sequencing: A1–A4+SP-1 → B1+B2+SP-2 → B3+B4. |
| 2026-06-10 | **Systems assessment + fix plan locked** (`docs/ASSESSMENT-2026-06-10.md`). Full-source review at Phase 3 close: architecture sound — orchestrator/lab/entity/descriptor layering clean, `@base/physics` hybrid implemented as locked. 3 defects: F1 health-pack rotation inert (per-instance `spawnCount` vs fresh-module-per-view), F2 ability CDs on wall-clock vs sim-time everywhere else, F3 map-load fallback renders void. Key sequencing call: **health/damage system (Phase 2-close item) pulled forward ahead of T-B18** — results screen has no stats without it; `hpGain`/`HudSnapshot.health`/`RoundSnapshot.elapsed` receptacles already exist. Plan: Tier A correctness fixes (A1 sim-time CDs, A2 module-scope rotation counter, A3 pre-load GLB before `super.onMount` so fallback is truthful, A4 comment nits) → B1 `HealthManager` + blob HP + `GameplayLabHost.recordDamage/recordKo` → B2 `RoundStats` → B3 T-B18 results overlay → B4 ability gating on round state. Triggered debt: C1 capsule probe (waist window), C2 target-system extraction from `DoomfistLab` (pre-champion-#2), C3 BVH, C4 fork-sync `GameplaySceneModule` disposal/reset port. Process: P1 merge branch→main + PR flow, P2 track `public/maps/chateau-guillard.glb`. Memory corrected: `castRayDown` is NOT used by Fix 2 (penetration-normal lift shipped instead); API kept dormant. |
| 2026-06-09 | **T-B17 round structure shipped.** `RoundManager` (countdown→playing→ended, 60s default) in `src/round/`. Wired into `DboxSceneModule.onAfterGameplayTick` via `tick(simDelta)` — simDelta is seconds (confirmed via HealthPackManager cooldown unit). `getRoundSnapshot()` polled each frame by `DboxView` rAF loop. Three overlays: countdown (big number, GO! flash on elapsed<0.8s), round timer top-center, end screen. Pointer lock released via `watch(roundSnapshot.state)` → `document.exitPointerLock()` — InputModule doesn't expose a public unlock API so direct browser call is correct. Restart via `router.push(menu)` + `router.push(dbox, arenaQuery)` detour — same-route `replace` does not remount Vue component; arenaQuery must be captured before first navigation since `route` is reactive and reflects menu route after push. |
| 2026-06-09 | **Spawn calibration complete.** `debugMarkers` survey of all 64 entity quads revealed entity type `0345` (OW spawn-volumes) are pure EMPTY nodes — no ≤8-tri mesh quads, so `renderDebugMarkers` cannot locate them. New approach: use confirmed ground-level entity marker positions as spawn points. Removed `otherparts-elevated-pickup (-30, 2)` — floor at Y≈5, above terrain probe start (Y=1), player would fall through; elevated spawns require higher probe, deferred. Final 4 spawn points: `yard-center (9.4, 1)`, `mainhall-interior-west (12.8, -12.3)`, `mainhall-interior-east (23.6, -12.1)`, `otherparts-ground-east (19.6, -12.6)`. `spawnFallback` updated to `(9, 1)`. 14 entity types fully annotated in descriptor. |
| 2026-06-08 | **Health pack substructure shipped.** Entity survey: `debugMarkers` enhanced to log parent entity node names; 64 marker spheres enumerated → 12 entity types classified. Entity type `37C2` ×2 = confirmed health packs (upper corridors, exact position match); `5120` ×1 = probable OtherParts-west pack; `51B9` ×3 = probable mainhall-upper cluster; `B9A8/3503/4FBx/4FEx` = boundary/structural (discarded). GLB entity node naming convention confirmed: `00000000<TypeHex4><Instance3>` (15-char). Substructure: `HealthPackExtractor.extractHealthPackSlots(root, slotDefs)` traverses Three.js scene for matching Object3D nodes, reads world positions via `getWorldPosition` after `updateWorldMatrix(true,true)`. `HealthPackManager` takes `ExtractedSlot[]` + rotation layouts; `mount(scene, rotationIndex)` activates a subset by group name; `tick(playerPos, dt)` returns HP gained (1.5m pickup radius, 30s cooldown). `HealthPackConfig` + `HealthPackSlotDef` added to `MapDescriptor`; Château Guillard wired with 3 slot groups (6 slots total) + 3 rotation layouts. `DboxSceneModule.spawnCount` persists across mounts to cycle rotations. Verified live: 6 slots extracted, 3/6 active on rotation 0 → `[corridor, otherparts-west]`. |
| 2026-06-08 | **OW map pipeline matured — registry + data-first descriptor + zone transparency + sanitization fix.** (1) `src/arenas/registry.ts` central arena registry: adding a new OW map reduced from 4-file edit to 2-file work (map descriptor + registry entry); `MenuView`/`DboxView` both derive from registry; routes use `RouteLocationRaw` named-route objects for base-path safety. (2) `MapDescriptorData` / `compileMapDescriptor()` data-first pattern: human-authored JSON-serializable descriptor with OWLib vocabulary (spawn zones, physics patterns, hidden type codes, entity annotations, display overrides); `compileMapDescriptor()` bridges to runtime `MapDescriptor` with pre-compiled RegExp — no RegExp in authored data. (3) Blender collection hierarchy extracted via headless Blender 5.0 (`extract_collections.py`) and baked into `BlenderCollection` tree in `chateauGuillard.ts` — documents collection roles, exclusive mesh hashes, instance counts. (4) Zone display overrides: second traversal pass in `DboxSceneModule.onMount()` runs on ALL meshes including Rapier-excluded background geometry; mountains + fog cubes hidden (`mesh.visible = false`). (5) **Critical Three.js finding**: `PropertyBinding.sanitizeNodeName()` strips dots/brackets/colons from GLTF node names — Blender `Submesh_0.HASH.443` → `Submesh_0HASH443`. All patterns updated: hash patterns use bare substring (no `\.HASH\.`), cube pattern changed to `^Cube\d*$`. Verified in browser: 13 meshes affected (up from 1). Applies to ALL Three.js GLTFLoader mesh-name pattern matching. |
| 2026-06-08 | **Blender-informed physics filter shipped** (via headless extraction). Mountain hash exclusion patterns, fog cube pattern, `smd_bone_vis` filter — all confirmed against Blender collection membership. `exclusiveMeshHashes` field on `BlenderCollection` documents which hashes are safe to exclude without re-export. Note: cloud/wateredgewaves share hash `81B09AB0F566B1D2` with castle walls — cannot distinguish; intentionally left in physics and rendering. |
| 2026-06-08 | **Tier 1 fixes SHIPPED.** Fix 1: dual-sphere Rapier probe in `DboxCharacterEntity` — feet (`y + radius`) + head (`y + 2×halfHeight − radius`) spheres; picks deeper hit from either probe, catches thin exterior walls single-centre sphere missed. Fix 2: normalY step-climb — if hit `normal.y > 0.3` (ramp/stair edge), snap `cy += depth × normal.y` instead of blocking XZ; carry slide guard added (`slideNx/Nz !== 0` required). Fix 3: `MapDescriptor` interface + `src/maps/chateauGuillard.ts` — spawn coords, `physicsFilter`, `owlibTechMat` centralised per-map; `DboxSceneModule.onMount` reads all config from descriptor. Architecture: `IAbilityLab` interface extracted; `DboxLab` class renamed `DoomfistLab implements IAbilityLab`; module now holds `IAbilityLab` ref for future multi-champion support. Build: `vue-tsc --noEmit` + `vite build` both clean. |
| 2026-06-06 | **Roadmap restructured.** Phase 2B (ability completeness) inserted between Phase 2 and Phase 3. Phase 3 reframed: OW map GLB (Château Guillard from Open3DLab as Track 1; Ecopoint via OWLib as Track 2) + `@base/physics` Rapier wrapper package. Original Phase 3 hand-authored arena geometry replaced by real OW map with Rapier trimesh collision. |
| 2026-06-07 | **T-B14 + T-B15 + T-B16 SHIPPED.** Château Guillard GLB acquired (Open3DLab, 136MB → 8.8MB draco+webp). `DboxSceneModule` loads map, adds Rapier trimesh (smd_bone_vis filter), uses flat Y=0 terrain sampler (OW map floor ≈ Y=0), `spherePenetration` handles walls. `DboxCharacterEntity` had missing `import * as THREE` (silent ReferenceError crash) — fixed. Arena select menu: `MenuView` Château Guillard (violet) + Sandbox (cyan), `DboxView` reads `?arena=` query param. |
| 2026-06-07 | **T-B16 cleanup pass.** Scene in OW map mode now fully sanitized: `useSandboxScene()` hook skips sandbox geometry; `spawnBlobs: !mapLoaded` opt skips NPC blobs; analytic arena wall planes/meshes replaced with empty arrays; fixture legend panel hidden via `!arenaId`. Terrain sampler upgraded from flat Y=0 to `MeshTerrainSampler` (Three.js CPU raycast from Y=5 → hits Château Guillard ground floor at Y≈−0.56, skipping rooftops at Y≈81+). All 3 abilities confirmed working in map mode (Rising Uppercut: 11.05m arc). |
| 2026-06-08 | **Interior spawn + OWLib mesh filtering complete.** Root cause of spawn-to-roof: `MeshTerrainSampler` probed from Y=500, hitting rooftop first every tick. Fix: `probeFromY=1.0` skips rooftop/box-tops, finds interior floor at Y=−0.969. `syncPosition` only writes internal delta state (NOT `character.position`) — must set both. OWLib technical meshes: ≤8-tri quads hidden-but-kept (floor detection); emissive/effect type prefixes (`13A_`/`9F_`/`9C_`/`9A_`/`B2_`/`10F_`/`F0_`) hidden. Remaining amber glass + protest banners are legitimate Château Guillard visual elements (`0_` standard material). |
| 2026-06-08 | **Floor sampler made character-relative — settled on `max(y+1, 1)` probe.** `probeFromY=1.0` missed floors above Y=1.0. `y-0.5` broke self-correction (probe below floor → missed → stuck). `y+2.0` found intermediate tech meshes at spawn (character.y=0, probe=2.0 hit wall-cap geometry). `y+1.0` is the correct balance: at spawn probe=max(1,1)=1.0 (identical to proven baseline, finds −0.969 interior floor), during navigation probe clamps to 1.0, on elevated surfaces tracks player up. `MeshTerrainSampler.probeFromY` accepts `number \| (() => number)` callback. Brick/architectural meshes were always in both Rapier + terrainMeshes; collision lists were never the bug. |
| 2026-06-08 | **OWLib material filter regex fixed.** Previous regex `/:(13A\|9F\|9C\|9A\|B2\|10F\|F0)_/` required a colon before the type code. OWLib GLB material names use `TypeCode_Hash` format (no colon), e.g. `13A_81B09AB0F566B1D2`. Fixed to `/\b(13A\|9F\|9C\|9A\|B2\|10F\|F0)_/` — word boundary fires at start-of-string and after non-alphanumeric separators without matching embedded hex substrings. Added `hiddenTechCount` debug log at load to confirm filter is working. |
| 2026-06-08 | **Iteration planning session — Tier 1 polish targets + scaling architecture locked.** Three blockers before T-B17: (1) Wall penetration — upgrade `DboxCharacterEntity` Rapier probe from single sphere to dual-sphere (feet + head) to catch thin exterior walls that single-centre probe misses; both `resolveCollision` and `resolveWalkingCollision` need updating. (2) Stair snag — if Rapier hit `normal.y > ~0.3` (shallow ramp / short step), snap player Y up by `depth × normal.y` instead of pushing XZ; preserves horizontal momentum. (3) Spawn calibration — `(30,−15)` confirmed heal-powerup locations, not player start; validated interior coords needed. Debug leftover found: `console.log("raycast", this)` at `DboxLab.ts:276` — fires every slam hold tick. Architecture for scaling: `MapDescriptor` interface consolidates per-map spawn coords + OWLib filter + mesh exclusion (moves hardcoded spawn+filter out of `DboxSceneModule.onMount`); `IAbilityLab` interface enables multi-champion (`DboxLab → DoomfistLab` rename, `DboxSceneModule` holds `IAbilityLab`); NPC enemy spawn positions flow from `MapDescriptor`. |
| 2026-06-08 | **OW map POC declared complete.** Floor collision + sampler stable; character navigates interior, all 3 abilities functional. Deferred to next session: wall penetration gaps (character can walk into tech/exterior area through thin walls), stair snag (step-climb tolerances too strict), spawn calibration (confirmed (30,−15) coords are heal-powerup locations, not player start). T-B17 round structure follows wall/stair polish. |
| 2026-06-07 | **Château Guillard interior spawn.** Floor/ceiling probes mapped topology: teal = outdoor courtyard (floor=−0.97, open sky), exterior box top (floor≈0, no walls). Both teal courtyard AND interior rooms share mesh `Submesh_2E0BE39370E446C37001` at floor=−0.97 — cannot distinguish by mesh name alone. Position (30,−15) visually confirmed as interior room (purple floor, tight walls N+E). `MAP_SPAWN_PROBE_XZ` reordered with (30,−15) first. Debug helpers `__teleport`/`__enclosure` removed. |
| 2026-06-07 | **T-B13 `@base/physics` SHIPPED.** `SHARED/packages/physics/` — `PhysicsWorld.create()` async factory (Rapier 0.14.0, module-level WASM init guard), `addStaticMesh(root: THREE.Object3D)` merges all Mesh children into a single fixed-body Rapier trimesh with world transforms applied, `spherePenetration(center, radius)` uses `world.projectPoint()` — exact drop-in contract for existing `CollisionResult` pattern in `DboxCharacterEntity`. Pure `tsc` build, WASM stays behind package boundary. |
| 2026-06-07 | **Phase 2 close deferred until after Phase 3 physics lands.** Health/damage numbers, hit-stop duration, and ability wall-reaction feel will all be affected by how Rapier responds to the OW map's geometry. Shipping Phase 2 now would require a second tuning pass after physics integration — skip Phase 2 and go Phase 3 first to avoid rework. Phase 2 items remain queued; resume after T-B15 is wired and testable. |
| 2026-06-06 | **`@base/physics` Rapier package architecture locked (planning only, no code).** Engine-agnostic public API (`PhysicsWorld`, `ColliderBuilder`, `ShapeCastResult`/`ColliderHandle` types). Rapier types hidden behind package boundary. Hybrid integration mode: Rapier owns collision geometry queries, `PlayerController` carry impulse system unchanged. `DboxCharacterEntity` gets optional `PhysicsWallCollider` param; falls back to existing plane/box math when absent. `PhysicsWorld.create()` async factory + `ColliderBuilder.fromGltfRoot()` + `overlapTest()` are the core query surface. |
| 2026-06-06 | **OW map asset pipeline planned (planning only, no code).** Track 1 (fastest): Château Guillard from Open3DLab → Blender → GLB export (`--compress draco`, not meshopt) → `public/maps/`. Track 2 (canonical): Ecopoint: Antarctica via OWLib DataTool + io_scene_owm Blender addon → same GLB pipeline. Ecopoint preferred as it is Doomfist's canonical lore arena. Boundary walls remain as analytic `WallPlane[]`; interior GLB geometry handled by Rapier trimesh. |

## Key constants (from DboxLab.ts)

| Constant | Value | Notes |
|----------|-------|-------|
| Punch CD | 4s | |
| Uppercut CD | 6s | |
| Slam CD | 6s | |
| Punch charge max | 1.4s | |
| Punch speed range | 78–152 m/s | Shaped charge curve (power 1.12) |
| Punch self-lift Vy | 1.65–3.15 | Scales with charge |
| Uppercut forward | 4 m/s | Carry impulse |
| Uppercut up | 26 m/s | Replace blend (wins over gravity) |
| Slam down | -24 m/s | Replace blend |
| Slam cone range | 7.25m | |
| Slam cone angle | 86 deg | |
| Carry decay | 8 /s | Exponential decay on planar carry |
| Skim-jump min carry | 22 m/s | Threshold to activate during punch |
