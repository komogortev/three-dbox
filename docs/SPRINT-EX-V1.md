# three-dbox — Sprint: Experience Floor → V1 Publish

> **Sprint goal:** take the "good base, far from the experience" build to a publishable V1 — an OW1 experience in limited form (single player, Score Attack + Practice Range), with a smooth/reliable/atmospheric/audible floor.
> **Created:** 2026-06-12. **Plan of record** for the session series. Each session reads this file first to find its position.
> Source plans: `PLAN-EX-EXPERIENCE-FLOOR-2026-06-12.md` · `PLAN-C2-CHAMPIONS-2026-06-12.md` · `GAP-V1-PUBLISH-2026-06-12.md` · `PLAN-SPAWN-2026-06-10.md` · `ASSESSMENT-2026-06-10.md` · `PLAN-EX-NAV-RESOLVER-2026-06-19.md`.

## Current position

➡️ **EX-2 architecture assessment + decision (2026-06-19): adopt Rapier `KinematicCharacterController`.** Root cause of why the floor won't calibrate: player Y is co-written every tick by two uncoordinated authorities — raycast `MeshTerrainSampler` hard-snap (no smoothing, `PlayerController.ts:1543`) + Rapier dual-sphere lift (`DboxCharacterEntity`) — reconciled only by scattered deadband constants, so every prior EX-2 fix was a referee tweak, not a model change. Decision: stop tuning constants; adopt Rapier's KCC (already shipped via `@dimforge/rapier3d-compat`) as one kinematic resolver — `computeColliderMovement` returns a single collide-and-slide + autostep + snap-to-ground + slope-corrected vector (carry still owns desired motion + gravity; HYBRID rule revised in `CLAUDE.md`). **Plan of record: `docs/PLAN-EX-NAV-RESOLVER-2026-06-19.md`.** **S0 SPIKE ✅ GO (run ~2026-06-21; re-verified + recorded 2026-07-05):** raw KCC `s0-kcc-spike.mjs` **22/22** (T0: `updateSceneQueries()` alone suffices in the never-stepped world — grounded=true, no `world.step()`; fall-catch; flat walk; autostep UP 0.25 m smooth; snap-to-ground DOWN 0.25 m smooth; wall block+slide; 3 m single-tick punch does NOT tunnel → EX-2.1 sweep deletable at S2) + compiled `CharacterMover` wrapper `s0-mover-validate.mjs` **6/6** + `s0-grounded-diag.mjs` isolation matrix (`FIX_INTERNAL_EDGES` makes grounding winding-sensitive: inverted winding → silent `grounded=false`; caveat documented in `addStaticMesh` JSDoc). ND-0 = YES. **Next: S1 — wire `CharacterMover` into map mode behind `?kcc` A/B → consolidated owner playtest (`docs/PLAYTEST-CHECKLIST-2026-07-05.md`).** Still owner-verify-pending from 2026-06-15 (committed `3751992`): floor-stumble + remount fixes — the KCC path supersedes the floor-tile/probe patches if S0 passes. Findings: `docs/EX-1-FINDINGS.md`.
**Floor-stumble + remount (2026-06-15, uncommitted WIP):** (a) raised the Rapier feet collision sphere `FOOT_PROBE_CLEARANCE = 0.12` m off the feet so OW floor-tile seams stop firing micro step-lifts / XZ pushes (the `MeshTerrainSampler` owns floor height; the feet sphere should only catch walls/steps). (b) **structural** remount fix (plan `~/.claude/plans/typed-crafting-wilkinson.md`): explicit `mapRoot.updateMatrixWorld(true)` barrier before the sampler/spawn (was an implicit `addStaticMesh`/render-timing side effect → could differ across mounts); spawn uses the unified `characterTerrainYOffset`, not a hardcoded `+0.85` (spawn == steady, kills a float-then-snap); a non-finite-floorY guard; a `?navdebug` SPAWN trace. Build clean; RAF-running headless verify shows sampler `gap=0` (grounded, stable). Owner verifies the remount visually via **Play Again ×N** (preserves `?navdebug`; menu cards drop it).
EX-2.2 (probe-Y) was tried and reverted: lowering the steady terrain-probe origin (`+1.0` above the head → `−0.30` below the capsule centre) to kill the stair/overhang pop **regressed grounding**. Confirmed live by A/B (reverting to `+1.0` restored a rock-stable `charY`, span 0; the `−0.30` build fell on spawn): a probe origin that tracks `position.y` from below chases the character DOWN on any dip and can't re-acquire the floor → grounding **diverges**, the avatar sinks/oscillates, the camera lurches — the owner-reported load **"flicker."** The high `+1.0` origin is self-correcting (~1.85 m recovery zone). Crouch breaks a low probe independently too (`DEFAULT_SKINNED_CROUCH_TERRAIN_Y_DELTA = −0.45` → crouched centre floorY+0.40). **The plan's "don't change the probe blind" caution was right.** A correct fix is **anti-divergent** — clamp the origin to the LAST KNOWN floor (don't chase `position.y`), or expose the controller's live ground estimate — and must be **owner-verified on stairs**. Net change this session = an in-source NOTE documenting the dead end. EX-2.1 anti-tunnelling stands.
EX-2.1 (anti-tunnelling) shipped: new additive **`PhysicsWorld.shapeCastSphere(from, to, radius)`** in `@base/physics` (Rapier `castShape` swept query) + a swept pre-pass in `DboxCharacterEntity.resolveCollision` gated on per-tick move > 0.5×radius (walking never pays it). **Unit-proven** via a thin-wall harness (8/8: detects the tunnel an overlap test misses, correct TOI/contact, push-out normal always faces the caster). Two non-obvious fixes the harness caught: (1) a never-stepped query world needs **`updateSceneQueries()`** after `addStaticMesh` or `castShape` returns null; (2) Rapier's cast normal sign isn't caller-facing — flip it against the sweep direction. dbox typecheck clean; Château map-mode smoke test clean (spawn correct, `posFinite`, no errors, swept code ticks without NaN). **The fall-through fix itself is owner-playtest** (a 152 m/s punch into a wall can't be driven headless — per the plan's capability table). (EX-2.2 was deferred from that session as a flagged bug-hunt; a 2026-06-14 attempt to lower the probe was REVERTED — see **Current position** — because it regressed grounding into a fall/flicker. The risk it named was real.)
Prior: EX-1.5 decimated `dfist_base.glb` 960,108 → 38,404 tris (skin weights preserved, mixer-verified). EX-1: lag driver was the un-decimated mesh, not draw calls; pixel-ratio cap shipped; BatchedMesh deferred.

## Pre-sprint gate *(resolved 2026-06-13)*

- [x] **P1 git:** PR #5 + PR #6 were **already merged** to `main` (commits `ffe080b`, `d27a961`) — `main` is unfrozen. The only open PR is **#7** (`docs/v1-ex-sprint-plan`, the plan-of-record docs); owner to merge when ready.
- [x] Branch `feat/ex1-perf-diagnosis` cut (off `docs/v1-ex-sprint-plan` so the sprint tracker + findings travel with the work; rebase onto `main` once #7 lands).
- Untracked alt-skin GLBs (`dfist.glb`, `dfist_armored.glb`) stay untracked by design (P2) — **also 33 MB / heavily un-decimated; optimise before ever activating.**

## Session series

Legend: ☐ not started · ◐ in progress · ☑ done. One session per row unless noted.

### Phase EX — experience floor *(this sprint's reason for existing)*

| # | Session | Entry | Exit criterion (live-verified) | Plan | Status |
|---|---|---|---|---|---|
| EX-1 | Perf + glitch diagnosis, renderer quick wins | pre-sprint gate done | `EX-1-FINDINGS.md` with draw-call/frame numbers; pixel-ratio cap shipped, no visual regression; stair/fall-through root-caused + instrumented; draw-call strategy decided (**defer BatchedMesh**) | EX §EX-1 | ☑ |
| EX-1.5 | **Character-mesh decimation** *(new — top EX-1 finding)* | EX-1 | `dfist_base.glb` 960K → ~30–50K tris via gltf-transform weld+simplify; rig + locomotion clips still play; live-verified | EX-1 findings §Decisions | ☑ |
| EX-2 | Navigation reliability | EX-1 numbers | full-charge punch into every thin wall stops (no tunneling); stairs + room transitions traverse without pops | EX §EX-2 → `PLAN-EX-NAV-RESOLVER-2026-06-19.md` | ◐ EX-2.1 anti-tunnel done (owner-playtest); EX-2.2 probe-Y tried + REVERTED (low origin regresses grounding → fall/flicker). **2026-06-19: root-caused = two Y-writers; adopting Rapier KCC (`PLAN-EX-NAV-RESOLVER-2026-06-19.md`). 2026-07-05: S0 ✅ GO (22/22 raw + 6/6 wrapper); S1 `?kcc` wire next** |
| EX-2b | *(contingency)* stair bug spillover / C1 capsule | only if EX-2 unresolved | — | EX §EX-2.2/2.3 | ☐ |
| EX-3 | Per-map lighting + Château dusk | EX-2 stable | lighting schema applied; Château reads as dusk courtyard (owner mood sign-off) | EX §EX-3 | ☐ |
| EX-4 | Audio foundation | — | ambient bed + ability/impact SFX + volume sliders working; placeholder/CC0 assets | EX §EX-4 | ☐ |

### Phase mechanics — V1 game systems *(unchanged internally; sequence locked)*

| # | Session | Entry | Exit criterion | Plan | Status |
|---|---|---|---|---|---|
| C2 | Target-system extraction + champion registry | EX track done | blobs → `TargetSystem` (no behavior change, live parity); `CHAMPION_REGISTRY`; per-arena counter keying | C2 plan | ☐ |
| B1+B2+SP-2 | Health/damage + stats + instant respawn | C2 done; **OD-1..OD-3 resolved** | real HUD HP; packs heal/skip-at-full; blob HP+KO; fall damage; respawn cycles; stats accumulate | Assessment §B + Spawn §SP-2 | ☐ |
| B3+B4 | Results screen + round gating + pause + crosshair | B1 done | menu→arena→play→results→menu loop; abilities gated to `playing`; pause overlay; reticle | Assessment §B3/B4 | ☐ |
| MAP-TGT | Targets on Château (Score Attack needs things to hit) | C2 done | map-mode target spawn set in `MapDescriptorData` → `TargetSystem` | GAP §2 | ☐ |

### Phase ship — V1 close *(juice + publish; some cuttable)*

| # | Session | Notes | Status |
|---|---|---|---|
| SHIP-1 | Hand Cannon (T-B8) + hit feedback (T-B10 shake/hit-stop) | hand cannon cuttable from min-V1 | ☐ |
| SHIP-2 | QA / tuning / publish pass | verify live GH-Pages URL + PWA install | ☐ |

## Open decisions (resolve at the named gate, not before)

| ID | Decision | Gate | Default |
|---|---|---|---|
| ~~draw-call~~ | ~~BatchedMesh vs GLB re-export join~~ → **RESOLVED 2026-06-13: defer BatchedMesh.** Draw calls (120–180 typ / 463 worst) are not the bottleneck; the 960K-tri character mesh is. Revisit only if courtyard worst-case still costs frames after EX-1.5. | ~~end of EX-1~~ | — |
| lighting | Château dusk mood | iterative in EX-3 | warm low-key sun + cool fill + fog |
| audio-assets | CC0 set vs synth placeholders | EX-4 | placeholders first, swap later |
| OD-1 | fall damage as V1 player damage source | before B1 | yes — 10/25/100 by landing tier |
| OD-2 | blob KO behaviour | before B1 | respawn in place ~8 s, `kos++` |
| OD-3 | ability damage numbers | before B1 | OW1 placeholders (uppercut 50, slam 50–125) |
| publish-posture | unlisted fan-demo vs asset-swap variant | before SHIP-2 | unlisted; revisit if going loud |

## Definition of done (sprint)

Visitor opens the deployed URL → installs PWA (optional) → picks Château **Score Attack** → moves smoothly with no fall-through/stair glitches → fights blobs with 3 abilities under map-appropriate dusk lighting with ability/impact audio → 60 s round → results screen with stats → Play Again. **Practice Range** available as mode 2. Single champion (Doomfist); champion select optional.

## Session-estimate envelope

Full V1 ≈ **9–12 sessions** (EX 4–5 · mechanics 3–4 · map-targets 1 · ship 1–2). Minimum honest V1 ≈ **8–9** (cut hand cannon/shields/VFX; EX track is not cuttable — it is the complaint list).

## Progress log

- **2026-07-05 (S0 verdict recorded + trees committed)** — The 2026-06-21 spike session had left `CharacterMover.ts` + 3 harnesses **uncommitted** in SHARED `feat/base-physics` with **no recorded verdict** (found at the 2026-07-05 doc sync). Re-ran all three harnesses against a fresh `tsc` build: `s0-kcc-spike.mjs` **22/22 → GO** — T0 diagnostic proves the never-stepped world needs `updateSceneQueries()` only (grounded=true, movement resolved; `world.step()` equivalent but unnecessary); autostep UP + snap-to-ground DOWN 0.25 m both smooth in per-tick Y-traces; wall block+slide correct; **3 m single-tick punch does not tunnel** (KCC swept by construction → EX-2.1 `shapeCastSphere` pre-pass deletable at S2, as the plan predicted). `s0-grounded-diag.mjs` isolation matrix: `FIX_INTERNAL_EDGES` makes grounding **winding-sensitive** (inverted winding → silent `grounded=false`; plain trimesh immune) — caveat documented on `addStaticMesh(opts.fixInternalEdges)`. `s0-mover-validate.mjs` **6/6** on the compiled dist wrapper (THREE.Vector3 in/out contract, autostep, snap, dispose). **ND-0 = YES.** Committed: SHARED `feat/base-physics` (`CharacterMover` + `PhysicsWorld.createCharacterMover` + `fixInternalEdges` opt + `extractTrimesh` filter + harnesses) and dbox `feat/ex1-perf-diagnosis` (plan doc + this tracker + CLAUDE.md HYBRID revision + `vue-tsc -b` scripts + STATE). Alt-skin GLBs (2×33 MB, untracked) deliberately NOT committed — need EX-1.5-style decimation first. Owner playtest checklist added: `docs/PLAYTEST-CHECKLIST-2026-07-05.md`. **Next: S1 `?kcc` wire.**
- **2026-06-19 (EX-2 nav architecture)** — **Assessment + plan, no runtime code.** Full-source read root-caused why EX-2 won't calibrate: player Y is co-written every tick by two uncoordinated authorities — raycast `MeshTerrainSampler` hard-snap (no smoothing, `PlayerController.ts:1543`) + Rapier dual-sphere lift (`DboxCharacterEntity`) — reconciled only by scattered deadband constants across four files; every prior EX-2 fix (`FOOT_PROBE_CLEARANCE`, `RAPIER_WALL_MIN_DEPTH`, probe-Y `+1.0`, the reverted EX-2.2) was a referee tweak, not a model change — hence non-convergence. Web research: Rapier's `KinematicCharacterController` (already shipped via `@dimforge/rapier3d-compat`) **is** the resolver — `computeColliderMovement` returns one collide-and-slide + autostep + snap-to-ground + slope-corrected vector → one writer; gravity stays the caller's job so the carry-impulse model is preserved. Cross-checked Godot/Unity/PhysX/BVHEcctrl — same standard knob set, nothing exotic. **Decision: adopt KCC** (not hand-build); HYBRID rule in `CLAUDE.md` revised to permit kinematic *resolution* (never dynamics). Plan of record `docs/PLAN-EX-NAV-RESOLVER-2026-06-19.md`: **S0 spike** (prove KCC in the never-stepped query world vs Château trimesh — `updateSceneQueries()`, no `world.step()` — go/no-go) → S1 wire behind `?kcc` with A/B → S2 delete the obsolete patchwork → S3 telemetry/tuning → S4 promote to `@base/physics`. Map-mode adopts KCC; procedural terrain keeps the sampler. **Next: S0 spike.**
- **2026-06-15 (floor-stumble + remount)** — **Two owner-reported issues fixed — IMPLEMENTED + owner-verify pending (uncommitted WIP).** *Floor stumble/stuck:* raised the Rapier feet collision sphere `FOOT_PROBE_CLEARANCE = 0.12` m off the feet (`DboxCharacterEntity`) so OW floor-tile seams stop firing micro step-lifts / XZ pushes — the `MeshTerrainSampler` owns floor height; the feet sphere is for walls/steps only (safe — only reduces near-floor collision sensitivity). *Spawn "under textures" on remount:* structural fix (approved plan `~/.claude/plans/typed-crafting-wilkinson.md`) — explicit `mapRoot.updateMatrixWorld(true)` barrier before the sampler/spawn (grounding had ridden on `addStaticMesh`→`extractTrimesh`'s `matrixWorld` side-effect + render timing, so it could differ first-mount vs remount); spawn uses the unified `characterTerrainYOffset` (feet-aligned = 0) instead of a hardcoded `+0.85` (spawn == steady, no float-then-snap); a non-finite-floorY guard; a `?navdebug` SPAWN trace; the `gap` formula fixed to the same offset. `vue-tsc`+vite clean; **headless verify with RAF actually running (60 fps) shows the sampler `gap=0`** (feet on floor, stable) — the earlier RAF-paused tab couldn't show this. Remount visual confirmation = **owner Play-Again ×N** (preserves `?navdebug`; menu cards drop the query). Discovered: headless raycast reconstructions hit an invisible SceneBuilder Y=0 ground-plane the real sampler excludes → trust the nav-log `gap`, not pixel reads (this contaminated the earlier "under textures" diagnosis). **Next: owner real-browser verify → EX-2.2 anti-divergence probe redesign.**
- **2026-06-14 (EX-2.2)** — **Probe-Y stair-pop fix ATTEMPTED → REVERTED (net: an in-source NOTE, no behaviour change).** Tried lowering the steady `MeshTerrainSampler` probe origin from `+1.0` (above the head) to `−0.30` below the capsule centre to stop the down-ray grabbing overhangs / upper-floor faces on stairs (the pop), with spawn kept on a separate one-shot high probe. **It regressed grounding** and was reverted to the known-good `+1.0`. Live A/B on the running dev build proved causation: with `−0.30` the character fell on spawn (camera Y descending 0.9 → −8 → −17.9 with no input); restoring `+1.0` gave a rock-stable `charY` (−0.97, span 0 over 6 samples). **Mechanism:** a probe origin `position.y + offset` with a *negative* offset tracks the player from below, so on any downward dip the origin falls WITH the character and the ray can no longer reach the floor above it → grounding diverges, the avatar sinks/oscillates, the camera lurches = the owner's load **"flicker."** The `+1.0` origin is self-correcting (~1.85 m recovery zone above the floor); that robustness — not the sampler's "below the ceiling" contract — is what keeps grounding stable. Crouch independently breaks a low probe (`DEFAULT_SKINNED_CROUCH_TERRAIN_Y_DELTA = −0.45` → crouched centre floorY+0.40). **This validates the plan's "don't change the probe blind" deferral.** A correct fix is **anti-divergent**: clamp the origin to `max(position.y + offset, lastFloorY + ε)` (track the last successful sample) so it can go low for ceiling/stair avoidance yet never drop below the floor it stands on — and it MUST be owner-verified on stairs (headless can't drive movement: the hidden tab pauses RAF). EX-2.1 anti-tunnelling is unaffected. **Next: anti-divergence probe redesign with owner stair-walking → step-lift smoothing if still needed → EX-3.**
- **2026-06-14 (EX-2.1)** — **Anti-tunnelling swept resolve shipped.** SHARED `@base/physics`: new additive `PhysicsWorld.shapeCastSphere(from, to, radius): { toi, normal } | null` wrapping Rapier `castShape` (Ball, maxToi=1 so toi ∈ [0,1]); `ShapeCastResult` type exported. **Required `world.updateSceneQueries()` in `addStaticMesh`** — a never-stepped query world doesn't populate the query pipeline for shape casts, so `castShape` returned null until added (the existing `spherePenetration`/`castRayDown` happen to work pre-update; `castShape` does not). **Normal-sign fix**: Rapier's cast normal isn't guaranteed caller-facing — flipped against the sweep direction so it's always a valid push-out/slide normal. Verified by a thin-wall esbuild harness (8/8: positive overlap control, swept hit toi=0.275 / contact z=−0.45, normal −z one way / +z reversed, clear-path null, zero-length null). dbox `DboxCharacterEntity.resolveCollision`: swept pre-pass gated on per-tick move > 0.5×radius (walking ≤0.09 m/tick never pays it) → on a wall hit (|normal.y| < 0.7) clamp to `from.lerp(to, toi)` + 0.02 m skin and feed the existing slide math; `lastPosition` recorded at end of `resolveWalkingCollision` (the tick's last resolve). physics rebuilt → dbox typecheck clean → Château map-mode smoke test clean (spawn `otherparts-ground-east`, `posFinite`, no error-level console, swept code ticks without NaN; 38,404-tri char). **Owner playtest needed** to confirm the fall-through is gone (152 m/s punch into a wall isn't drivable headless). **Next: EX-2.2** probe-Y stair fix (`+1.0` → `−0.5` with a one-shot spawn probe) + step-lift smoothing — the flagged bug-hunt; do it with owner stair-walking in the loop. (Note: SHARED branch `feat/base-physics` also carries pre-existing unrelated `extractTrimesh.ts` WIP — left untouched.)
- **2026-06-13 (EX-1.5)** — **Character-mesh decimation shipped.** `public/models/dfist_base.glb` decimated **960,108 → 38,404 tris** (gltf-transform `weld` → `simplify --ratio 0.04 --error 0.01` → `draco`; 3.05 MB → 639 KB, −79%). Skin weights + `JOINTS_0`/`WEIGHTS_0` preserved; 40-bone skeleton bound and **mixer-confirmed live** (`/dbox` sandbox via the `?perf` `window.__dboxPerf` handle: 38,404-tri SkinnedMesh, non-identity bone quaternions = idle clip driving the rig, clean recognizable Doomfist deformation, sane local bbox 0.46×1.0×1.0 = no broken-weight explosion, no console errors; whole-scene frame 60K tris / 54 draws). Kept **Draco** (meshopt position-quantization breaks GPU skinning per the `dbox.ts` note); textures untouched; **no normals added** (faithful to the original flat-shaded attribute set — revisit when EX-3 redesigns lighting). `prune` was a correct no-op (runtime char subtree is a clean 44 nodes — 40 bones/1 mesh/3 empties; the raw file's many unused *scenes* never load). Original backed up at `/tmp/dfist_base.original.glb` + git. Alt-skins `dfist.glb`/`dfist_armored.glb` (33 MB each, untracked) still need the same pass before they're ever activated. **Next: EX-2 navigation reliability.**
- **2026-06-13** — **EX-1 shipped** (`feat/ex1-perf-diagnosis`). Flag-gated diagnostics (`src/debug/diagnostics.ts`): `?perf` 1 Hz `renderer.info` overlay + `window.__dboxPerf` handle, `?navdebug` per-tick nav logger; `DboxCharacterEntity.lastStepLiftY`/`lastWallPushDepth`. Renderer quick win: **pixel-ratio cap `min(dpr,2)` shipped** (`?pixelratio=N` override) + tone/shadow audit logged. Live-measured via the handle (build-independent figures). **Findings revised the hypothesis** (`docs/EX-1-FINDINGS.md`): #1 lever is the **960K-tri `dfist_base.glb` player mesh (73% of geometry)**, not draw calls (120–180 typ / 463 worst); render is vertex-bound on the dev GPU (flat across pixelRatio → cap is hi-DPI insurance). Probe-Y stair bug + `spherePenetration` tunnelling both root-caused by code for EX-2. Lighting baseline captured (inherited sandbox noon atmosphere). Bonus: found a latent `TouchProvider` mount-side container-collapse (0×0 canvas) bug. **Decisions: defer BatchedMesh; new EX-1.5 char-mesh decimation task inserted before EX-2.**
- **2026-06-12** — Sprint created. Planning session: confirmed multi-champion near-term (C2-first), assessed gap-to-V1, folded owner playtest verdict into the EX experience-floor track ahead of all mechanics work, wrote EX implementation plan (all APIs direct-read-verified). No code. Next: pre-sprint git gate → EX-1.
